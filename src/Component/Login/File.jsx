import React from 'react';
import { useLocation } from 'react-router-dom';

const File = () => {
  const location = useLocation();
//   const pathParts = location.pathname.split('/'); // Split pathname into parts
//   const boxNumber = pathParts[pathParts.lengthgit ad. - 1]; // Last part is the boxNumber

  return (
    <div>
      <h2></h2>
   
      {/* Add your file page content here */}
    </div>
  );
};

export default File;
